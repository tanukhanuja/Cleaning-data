#Steps in Data analysis: (Raw data -> Processing script -> Tidy data) -> data analysis -> data communication
##Data cleaning needs 4 things:
        #1. The raw data
        #2. A tidy dataset
        #3. metadata: A code book describing each variable and its values in tidy dataset
        #4. Recipe used to go from above 1,2,3 points